<div class="container">
  <div class="row">
	<h3>Tumblebit (Alice) Puzzle # <%= @payment.id %></h3>
	<p>P2SH Address: <strong><%= @payment.hash_address %></strong></p>

	<p>Expiry: <strong><%= Time.at(@payment.expiry_date.to_time.to_i).utc %></strong></br>
	Expiry in seconds since epoch time (1/1/1970): <%= @payment.expiry_date.to_time.to_i %></p>
	
	<p>Real indices to share now with Tumbler: <strong><%= @payment.real_indices %></strong></p>
	
		<p><a href="<%= products_path("ro_values_#{@payment.hash_address}.csv") %>" class="btn btn-lg btn-primary heading__btn wow fadeInUp" >Download File with ro_values for Tumbler </a></p>
	
	</div>
</div>
