<div class="container">
  <div class="row">
    <h3> Puzzles In Progress</h3>
    <div class="column">
		<%= form_tag(posts_path, :method => "get", id: "search-form") do %>
		<%= text_field_tag :search, params[:search], placeholder: "Search Puzzle by y" %>
		<%= submit_tag "Search" %>
		<% end %>
      <table class="table">
        <tbody>
          <% @puzzles.each do |puzzle| %>
            <tr>
              <%= render puzzle %>
            </tr>
          <% end %>
        </tbody>
      </table>
	  <div class="apple_pagination">
		<%= will_paginate @puzzles %>
	  </div>
    </div>
  </div>
</div>