<div class="container">
	<div class="row">
	<% content_for :title do %>Opcodes<% end %>
	<!-- Posts are created in db/seeds.rb -->
	<% @post = Post.find_by_title("Opcodes") %>
	<%= markdown(@post.body) %>
	</br>
	</div>
</div>
