<div class="container">
	<div class="row">
	<% content_for :title do %>CHECKLOCKTIMEVERIFY<% end %>
	<!-- Posts are created in db/seeds.rb -->
	<% @post = Post.find_by_title("CHECKLOCKTIMEVERIFY") %>
	<%= markdown(@post.body) %>
	</br>
	</div>
</div>
