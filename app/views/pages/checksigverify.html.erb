<div class="container">
	<div class="row">
	<% content_for :title do %>CHECKSIGVERIFY<% end %>
	<!-- Posts are created in db/seeds.rb -->
	<% @post = Post.find_by_title("CHECKSIGVERIFY") %>
	<%= markdown(@post.body) %>
	</br>
	</div>
</div>
